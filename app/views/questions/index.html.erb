<section class="container mt-5">
	Question : 
	<% @questions.each do |question| %>
		<%= question.content %>
		<%= link_to "New",new_course_question_path, class:'btn btn-success' %>
	<% end %>
</section>