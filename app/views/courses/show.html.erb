<h1>Class</h1>
  Title : 
  <%= @course.title %> <br />
  Description :
  <%= @course.description %> <br />
<% if current_student == nil %>
  <container class="row">

    <%= link_to "Edit",edit_course_path(@course.id), class:'btn btn-success' %>
    <%= link_to "Delete",course_path(@course.id), class:'btn btn-danger', method: :delete %>
    <section>
      <%= render partial: "courses/show/teacher/teacher_steps" %>
    </section>
    <section>
      <%= render partial: "courses/show/teacher/teacher_questions" %>
    </section>
  </container>
<% end %>

<% if current_teacher == nil %>
  <container class="row">
    <section>
      <%= render partial: "courses/show/student/student_steps" %>
    </section>
    <section>
      <%= render partial: "courses/show/student/student_questions" %>
    </section>
  </container>
<% end %>

<h1>Questions</h1>
<% if Question.where(course_id: @course.id).empty? == false %>
  Content : <br>
  <% Question.where(course_id: @course.id).pluck(:content).each do |content|%>
    <%= content %>
      <%# if Student.find(Question.where(content: content).ids) == current_student.id%>
        <%= link_to "Edit",edit_course_question_path(@course.id, Question.where(content: content).ids), class:'btn btn-success' %>
        <%= link_to "Delete",course_question_path(@course.id, Question.where(content: content).ids), class:'btn btn-danger', method: :delete %><br><br>
      <%# end %>
  <%end%>
<% else %>
  No question yet !
<% end %>

<!--  <div class="col-sm-10">
    <%# form_for Question.new, url: course_questions_path(@course.id), method: :post do |f| %>
        <%# f.text_field :content, class:"form-control form-control-lg", id:"colFormLabelLg", placeholder:"Type your question..."%>
        <%# f.submit class:"btn btn-primary mb-2" %>
    <%# end %>
  </div> 


  <%# form_tag(course_questions_path(@course), authenticity_token: true, method: 'POST', multipart: true) do%>
        <div class="row form-group card-body text-primary">
          <div class="col-8 text-right">
           <%# text_field_tag 'question[content]', '', class: ' form-control ', placeholder: 'Type your question...'%>
          </div>
        <div class="col-4 text-right">
         <%# submit_tag "Ask question", class: " btn btn-success" %>
        </div>
        </div>
      <%#end%> -->